
  # TrainerX64

  This is a code bundle for TrainerX64. The original project is available at https://www.figma.com/design/EPjdIjk0hgVz5wb7O2iz8b/TrainerX64.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  